# Word Helper 项目 - 完整部署与维护手册

> **版本**: 2.1.0  
> **更新日期**: 2026-05-23  
> **适用版本**: Word Helper v1.0+  
> **推荐部署组合**: Replit（后端）+ Neon（数据库）+ Vercel（前端）  
> **重要**: 全部免费，不需要绑定信用卡！

---

## 📋 目录

1. [项目概述](#项目概述)
2. [部署架构](#部署架构)
3. [前置准备](#前置准备)
4. [第一步：部署 Neon PostgreSQL 数据库](#第一步部署-neon-postgresql-数据库)
5. [第二步：部署后端到 Replit](#第二步部署后端到-replit)
6. [第三步：部署前端到 Vercel](#第三步部署前端到-vercel)
7. [环境变量配置](#环境变量配置)
8. [数据库初始化](#数据库初始化)
9. [访问和测试](#访问和测试)
10. [维护指南](#维护指南)
11. [常见问题排查](#常见问题排查)
12. [成本说明](#成本说明)
13. [重要提醒](#重要提醒)

---

## 项目概述

### 什么是 Word Helper？

Word Helper 是一个在线单词学习工具，支持：
- PDF 单词导入
- 智能分类（词根词缀）
- 错题复习
- 随手拼练习模式

### 技术栈

| 组件 | 技术 | 托管平台 | 费用 | 是否需要绑卡 |
|------|------|----------|------|-------------|
| **前端** | Vue 3 + Vite | Vercel | 免费 | ❌ 不需要 |
| **后端** | Node.js + Express + TypeScript | Replit | 免费 | ❌ 不需要 |
| **数据库** | PostgreSQL 17 | Neon | **永久免费** | ❌ 不需要 |

### 选择这套方案的原因

1. **完全免费**：所有平台都不需要信用卡
2. **Neon 永久免费**：不像 Render PostgreSQL 有 90 天过期限制
3. **访问速度快**：全球节点
4. **零成本**：适合个人项目

---

## 部署架构

```
┌─────────────────────────────────────────────────────────────┐
│                        用户浏览器                             │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                    Vercel (前端)                            │
│               https://word-helper.vercel.app               │
│                    Vue 3 + Vite                            │
└────────────────────────┬────────────────────────────────────┘
                         │ API 请求
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                    Replit (后端 API)                        │
│              https://word-helper.username.repl.co          │
│                   Node.js + Express                        │
└────────────────────────┬────────────────────────────────────┘
                         │ PostgreSQL 连接
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                   Neon PostgreSQL                           │
│              永久免费 · 3GB 存储 · 亚太节点                 │
│              ep-cool-breeze-xxxx.us-east-2.aws.neon.tech  │
└─────────────────────────────────────────────────────────────┘
```

---

## 前置准备

### 需要的账号

1. **GitHub 账号** - 代码托管
2. **Neon 账号** - 数据库（永久免费，不需要绑卡）
3. **Replit 账号** - 后端部署（免费，不需要绑卡）
4. **Vercel 账号** - 前端部署（免费，不需要绑卡）

### 账号注册链接

| 平台 | 注册地址 | 是否需要绑卡 |
|------|----------|-------------|
| GitHub | https://github.com | ❌ 不需要 |
| Neon | https://neon.tech | ❌ 不需要 |
| Replit | https://replit.com | ❌ 不需要 |
| Vercel | https://vercel.com | ❌ 不需要 |

### 代码准备

确保代码已推送到 GitHub：

```bash
cd word-helper
git add .
git commit -m "准备部署"
git push origin main
```

---

## 第一步：部署 Neon PostgreSQL 数据库

> **重要**：Neon 提供永久免费 PostgreSQL 数据库，**没有过期限制**，不用担心数据丢失。

### 1.1 注册 Neon 账号

1. 访问 [https://neon.tech](https://neon.tech)
2. 点击 **Sign Up** 按钮
3. 选择 **GitHub** 登录（最简单）
4. 授权 GitHub 访问

### 1.2 创建数据库项目

1. 登录后，点击 **New Project**

2. 配置项目信息：

| 配置项 | 推荐值 | 说明 |
|--------|--------|------|
| **Project Name** | `word-helper` | 项目名称 |
| **PostgreSQL Version** | `17` | 最新稳定版 |
| **Region** | `US East (Ohio)` | 美东节点 |
| **Compute Size** | `0.25 vCPU` | 免费版默认 |

3. 点击 **Create Project**

### 1.3 保存数据库凭证

> ⚠️ **重要**：密码只会显示一次，请立即保存！

创建项目后，会显示连接信息：

```
🎉 Your project is ready!

Connection Details:
- Host: ep-cool-breeze-123456.us-east-2.aws.neon.tech
- Database: neondb
- User: neondb_owner
- Password: npg_xxxxxxxxxxxxxxx

Connection string:
postgresql://neondb_owner:npg_xxxxxxxxxxxxxxx@ep-cool-breeze-123456.pooler.us-east-2.aws.neon.tech/neondb?sslmode=require
```

**保存以下信息**：
- ✅ 连接字符串（Connection string）
- ✅ 数据库密码

### 1.4 Neon 免费版限制

| 项目 | 免费额度 | 说明 |
|------|----------|------|
| **存储空间** | 3 GB | 超出需付费 |
| **分支数据库** | 支持 | 可以创建测试分支 |
| **自动休眠** | ❌ 不会 | 随时可访问 |
| **SSL** | ✅ 必须 | 自动配置 |

---

## 第二步：部署后端到 Replit

> **Replit 优点**：不需要绑卡，浏览器IDE，一键部署

### 2.1 登录 Replit

1. 访问 [https://replit.com](https://replit.com)
2. 点击 **Sign Up**
3. 选择 **GitHub** 登录（不需要绑卡）

### 2.2 创建新项目

1. 点击 **+ Create Repl**
2. 选择 **Import from GitHub**
3. 输入仓库地址（你的 GitHub 仓库 URL）
4. 点击 **Import from GitHub**

### 2.3 配置环境

1. **等待项目导入**（约 1-2 分钟）

2. **配置环境变量**:
   - 点击左侧边栏的 **Secrets**
   - 点击 **Add Secret**
   - 添加以下变量：

| 变量名 | 值 | 说明 |
|--------|-----|------|
| `DATABASE_URL` | [你的 Neon 连接字符串] | 从 Neon 复制 |
| `PGSSLMODE` | `require` | SSL 模式 |

3. **配置 .replit 文件**:
   - 点击左侧边栏的 **Files**
   - 找到并打开 `.replit` 文件
   - 修改为：

```
# .replit
run = "cd backend && npm install && npm run build && npm start"
entrypoint = "backend/dist/index.js"

[env]
DATABASE_URL = "$DATABASE_URL"
PGSSLMODE = "$PGSSLMODE"
```

### 2.4 运行项目

1. 点击上方的 **Run** 按钮
2. 等待安装依赖和构建（约 2-3 分钟）
3. 构建成功后，会显示：
   ```
   Server running on port 3000
   ```

### 2.5 获取后端地址

Replit 会为你的项目分配一个 URL，格式为：
```
https://word-helper-你的用户名.repl.co
```

**保存这个地址**，前端部署时需要用到。

### 2.6 Replit 免费版限制

| 限制项 | 说明 |
|--------|------|
| **内存** | 512MB |
| **CPU** | 0.2 vCPU |
| **休眠** | 1小时无请求自动休眠 |
| **带宽** | 每月 100GB |

---

## 第三步：部署前端到 Vercel

### 3.1 登录 Vercel

1. 访问 [https://vercel.com](https://vercel.com)
2. 点击 **Sign Up**
3. 选择 **GitHub** 登录（不需要绑卡）

### 3.2 导入项目

1. 点击 **Add New** → **Project**
2. 选择 `word-helper` 仓库
3. Vercel 会自动检测前端配置

### 3.3 配置构建设置

Vercel 自动配置：
- **Framework Preset**: `Vite`
- **Build Command**: `npm run build`
- **Output Directory**: `dist`

### 3.4 配置环境变量

在 **Environment Variables** 部分添加：

| 变量名 | 值 | 说明 |
|--------|-----|------|
| `VITE_API_URL` | `https://word-helper-你的用户名.repl.co` | Replit 后端地址 |

### 3.5 部署

1. 点击 **Deploy**
2. 等待构建完成（约 1-2 分钟）
3. 部署成功后，获得前端地址

### 3.6 配置路由重写

创建 `vercel.json` 文件：

```json
{
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
```

---

## 环境变量配置

### 后端环境变量（Replit）

| 变量名 | 示例值 | 说明 |
|--------|--------|------|
| `DATABASE_URL` | `postgresql://user:pass@ep-xxx.neon.tech/neondb?sslmode=require` | 数据库连接 |
| `PGSSLMODE` | `require` | SSL 连接模式 |

### 前端环境变量（Vercel）

| 变量名 | 示例值 | 说明 |
|--------|--------|------|
| `VITE_API_URL` | `https://word-helper-username.repl.co` | 后端 API 地址 |

---

## 数据库初始化

### 自动初始化

后端服务首次启动时，会自动创建所有必要的表。

### 手动初始化（可选）

在 Replit 的 **Shell** 中执行：
```bash
cd backend
npm run build
npm start
```

---

## 访问和测试

### 访问地址

| 服务 | 地址 | 说明 |
|------|------|------|
| **前端** | `https://word-helper.vercel.app` | Vercel 分配 |
| **后端 API** | `https://word-helper-username.repl.co` | Replit 分配 |
| **健康检查** | `https://word-helper-username.repl.co/health` | API 状态检查 |

---

## 维护指南

### 1. 代码更新

#### Replit 更新

1. 在 GitHub 推送代码
2. 在 Replit 点击 **Pull from GitHub**
3. 点击 **Run** 重新部署

#### Vercel 更新

Vercel 会自动检测 GitHub 推送并部署。

### 2. 防止 Replit 休眠

免费版会在 1 小时无请求后休眠，可用以下方法：

#### 使用 UptimeRobot

1. 访问 https://uptimerobot.com
2. 创建免费账号（不需要绑卡）
3. 添加监控：
   - **URL**: `https://word-helper-username.repl.co/health`
   - **Interval**: 30 分钟

### 3. 数据库备份

#### 使用 pg_dump

```bash
pg_dump -h ep-xxx-xxx.us-east-2.aws.neon.tech \
  -U neondb_owner \
  -d neondb \
  -f backup.sql
```

---

## 常见问题排查

### Q1: Replit 部署失败

**症状**：构建失败或服务无法启动

**解决方法**：
1. 查看 Replit 的 **Console** 日志
2. 确认环境变量已正确配置
3. 检查 `DATABASE_URL` 是否正确

### Q2: CORS 错误

**症状**：前端无法访问后端 API

**解决方法**：
1. 后端已配置支持 Replit 和 Vercel 域名
2. 确认前端 `VITE_API_URL` 正确
3. 重新部署前端

### Q3: Replit 服务休眠

**症状**：首次访问需要等待很久

**解决方法**：
1. 使用 UptimeRobot 定时唤醒
2. 手动访问一次唤醒服务

### Q4: 数据库连接失败

**症状**：后端无法连接数据库

**解决方法**：
1. 确认 `DATABASE_URL` 正确
2. 确认 Neon 项目状态正常
3. 检查 SSL 配置

---

## 成本说明

### 当前部署成本

| 服务 | 费用 | 是否需要绑卡 |
|------|------|-------------|
| **Neon** | ¥0 | ❌ 不需要 |
| **Replit** | ¥0 | ❌ 不需要 |
| **Vercel** | ¥0 | ❌ 不需要 |

**总计**: ¥0/月，完全免费！

---

## 重要提醒

### ⚠️ Replit 休眠问题

- 免费版 1 小时无请求会休眠
- 休眠后首次访问需要约 10 秒启动
- 建议使用 UptimeRobot 保持活跃

### ⚠️ 数据备份

- 定期导出数据库备份
- 重要数据不要只存一份

---

## 快速参考

### 常用命令

```bash
# 推送代码到 GitHub
git add .
git commit -m "更新"
git push origin main

# Replit 更新
# 在 Replit 中点击 "Pull from GitHub"

# 本地测试
cd backend
npm install && npm run build && npm start
```

### 重要地址

| 服务 | 地址 |
|------|------|
| **Neon Dashboard** | https://console.neon.tech |
| **Replit Dashboard** | https://replit.com/~ |
| **Vercel Dashboard** | https://vercel.com/dashboard |

---

**祝部署顺利！** 🎉

---

*文档版本：2.1.0*
*最后更新：2026-05-23*
