# Word Helper 项目 - 部署维护手册

> **版本**: 5.0.0  
> **更新日期**: 2026-05-24  
> **适用版本**: Word Helper v1.0+  

---

## 📋 目录

1. [项目概述](#项目概述)
2. [阿里云服务器部署](#阿里云服务器部署)
3. [一键启动脚本](#一键启动脚本)
4. [环境变量配置](#环境变量配置)
5. [维护指南](#维护指南)

---

## 项目概述

### 技术栈

| 组件 | 技术 |
|------|------|
| **前端** | Vue 3 + Vite |
| **后端** | Node.js + Express + TypeScript |
| **数据库** | PostgreSQL 17 |

---

## 阿里云服务器部署

### 前置准备

1. **开放安全组端口**：
   - 22 (SSH)
   - 3000 (项目)
   - 5432 (PostgreSQL)

2. **连接服务器**：
   ```bash
   ssh root@<你的服务器IP>
   ```

### 一键部署

在服务器终端中执行：

```bash
# ============ 一键部署脚本 ============
apt update && apt upgrade -y
apt install -y git docker.io docker-compose postgresql postgresql-contrib

# 设置 PostgreSQL 密码
sudo -u postgres psql -c "ALTER USER postgres PASSWORD '!henji2168Carlos';"
sudo -u postgres psql -c "CREATE DATABASE wordhelper;"

# 克隆项目
cd /opt
git clone https://github.com/CarlosShao/word-helper.git
cd word-helper

# 创建环境变量
cat > .env << 'EOF'
DATABASE_URL=postgresql://postgres:!henji2168Carlos@localhost:5432/wordhelper
PGSSLMODE=disable
PORT=3000
EOF

# 启动项目
docker-compose up -d

# 查看状态
docker-compose ps
echo "访问地址: http://<你的IP>:3000"
# ============ 脚本结束 ============
```

---

## 一键启动脚本

### start.sh

```bash
#!/bin/bash
# Word Helper 一键启动脚本

echo "=== Word Helper 启动脚本 ==="

# 检查是否存在 .env 文件
if [ ! -f .env ]; then
    echo "创建 .env 文件..."
    cat > .env << 'EOF'
DATABASE_URL=postgresql://postgres:!henji2168Carlos@localhost:5432/wordhelper
PGSSLMODE=disable
PORT=3000
EOF
fi

# 启动服务
echo "启动 Docker 容器..."
docker-compose up -d

# 等待启动完成
sleep 10

# 检查状态
echo "=== 服务状态 ==="
docker-compose ps

# 显示访问地址
echo ""
echo "=== 访问地址 ==="
echo "前端: http://$(hostname -I | awk '{print $1}'):3000"
echo "后端 API: http://$(hostname -I | awk '{print $1}'):3000/api"
echo ""
echo "=== 日志查看 ==="
echo "docker-compose logs -f"
```

### 使用方法

```bash
# 赋予执行权限
chmod +x start.sh

# 启动
./start.sh

# 停止
docker-compose down
```

---

## 环境变量配置

### .env 文件

```env
# 数据库连接
DATABASE_URL=postgresql://postgres:!henji2168Carlos@localhost:5432/wordhelper

# SSL 模式（本地数据库设为 disable）
PGSSLMODE=disable

# 端口
PORT=3000
```

---

## 维护指南

### 常用命令

```bash
# 启动服务
docker-compose up -d

# 停止服务
docker-compose down

# 查看日志
docker-compose logs -f

# 更新代码
cd /opt/word-helper
git pull origin main
docker-compose down && docker-compose up -d

# 数据库备份
pg_dump -U postgres wordhelper > backup.sql

# 数据库恢复
psql -U postgres wordhelper < backup.sql
```

### 故障排查

| 问题 | 解决方法 |
|------|----------|
| 端口占用 | `lsof -i :3000` 查看并杀死进程 |
| 数据库连接失败 | 检查 PostgreSQL 服务状态 |
| Docker 启动失败 | 检查 Docker 服务 `systemctl status docker` |

---

## 🎉 部署成功！

访问地址：`http://<你的服务器IP>:3000`

---

*文档版本*: 5.0.0  
*最后更新*: 2026-05-24
