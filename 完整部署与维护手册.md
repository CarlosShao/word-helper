# Word Helper 项目 - 完整部署与维护手册

> **版本**: 9.1.0  
> **更新日期**: 2026-05-30  
> **适用版本**: Word Helper v1.0+  
> **目标读者**: 开发者、运维人员

---

## 📋 目录

1. [必读！开始之前](#必读开始之前)
2. [方案选择指南](#方案选择指南)
3. [方案1：Linux 一键部署（推荐）](#方案1linux-一键部署推荐)
4. [方案2：直接部署（不用 Docker）](#方案2直接部署不用-docker)
5. [环境配置说明](#环境配置说明)
6. [GitHub OAuth 配置](#github-oauth-配置)
7. [SMTP 邮件服务配置](#smtp-邮件服务配置)
8. [常用维护命令](#常用维护命令)
9. [常见问题解决](#常见问题解决)

---

## 必读！开始之前

### 你需要准备什么

| 项目 | 说明 | 状态 |
|------|------|------|
| 服务器 | 阿里云/腾讯云等 Linux 服务器 | ✅ |
| 公网 IP | 例如：182.92.108.240 | ✅ |
| root 密码 | 服务器登录密码 | ✅ |
| 安全组开放端口 | 22, 3000 | ✅ |

### 开放安全组端口（重要！）

在云控制台操作：
1. 登录云控制台 → ECS 实例 → 安全组
2. 点击「配置规则」→「入方向」→「添加安全组规则」

| 端口 | 用途 | 协议 | 授权对象 |
|------|------|------|---------|
| 22 | SSH 连接 | TCP | 0.0.0.0/0 |
| 3000 | 项目网站 | TCP | 0.0.0.0/0 |

---

## 方案选择指南

| 方案 | 优点 | 缺点 | 难度 |
|------|------|------|------|
| **方案1：Linux 一键部署** | ✅ 自动处理网络问题<br>✅ 支持 GitHub/Gitee 双仓库<br>✅ 自动配置环境 | ⚠️ 需要执行脚本 | ⭐⭐ |
| **方案2：直接部署** | ✅ 最简单，零网络问题<br>✅ 国内服务器完美兼容 | ⚠️ 需要手动配置环境 | ⭐⭐⭐ |

---

## 方案1：Linux 一键部署（推荐）

### 适用情况
✅ 国内服务器无法访问 Docker Hub  
✅ 希望一键完成部署  
✅ 支持自动切换 GitHub/Gitee 仓库

### 步骤1：下载并执行一键部署脚本

```bash
# 1. 克隆项目（优先 GitHub，失败自动切换 Gitee）
cd /opt
if timeout 3 git clone https://github.com/CarlosShao/word-helper.git 2>/dev/null; then
    echo "✅ 从 GitHub 克隆成功"
else
    echo "⚠️ GitHub 超时，尝试 Gitee..."
    git clone https://gitee.com/CarlosShao/word-helper.git
fi

cd word-helper

# 2. 运行一键部署脚本
chmod +x start.sh
./start.sh
```

### 脚本功能说明

#### start.sh - 一键启动脚本

`start.sh` 脚本会自动完成：
1. ✅ 检查 Docker 状态并启动
2. ✅ 尝试从 GitHub 获取代码，失败自动切换到 Gitee
3. ✅ 清理旧容器和镜像
4. ✅ 构建并启动服务

#### stop.sh - 一键停止脚本

`stop.sh` 脚本会自动完成：
1. ✅ 停止 Docker Compose 服务
2. ✅ 停止 word-helper 容器
3. ✅ 删除 word-helper 容器

### 快速使用命令

```bash
# 一键启动服务（推荐）
./start.sh

# 一键停止服务
./stop.sh

# 查看服务日志
docker-compose logs -f

# 重启服务（先停止再启动）
./stop.sh && ./start.sh
```

### 访问网站

```
http://你的服务器IP:3000
```

---

## 方案2：直接部署（不用 Docker）

### 适用情况
✅ 不想使用 Docker  
✅ 需要更灵活的环境配置

### 步骤1：安装依赖

**CentOS/Alibaba Cloud Linux：**
```bash
# 更新系统
yum update -y

# 安装 Node.js（国内源）
curl -fsSL https://rpm.nodesource.com/setup_20.x | sudo bash -
sudo yum install -y nodejs git

# 安装 pm2
npm install -g pm2
```

**Ubuntu/Debian：**
```bash
# 更新系统
apt update && apt upgrade -y

# 安装 Node.js
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo bash -
sudo apt install -y nodejs git

# 安装 pm2
npm install -g pm2
```

### 步骤2：克隆项目

```bash
cd /opt
if timeout 3 git clone https://github.com/CarlosShao/word-helper.git 2>/dev/null; then
    echo "✅ 从 GitHub 克隆成功"
else
    git clone https://gitee.com/CarlosShao/word-helper.git
fi

cd word-helper/backend
```

### 步骤3：安装依赖并构建

```bash
npm install --registry=https://registry.npmmirror.com --legacy-peer-deps
npm run build
```

### 步骤4：配置环境变量

```bash
# 使用项目自带的 .env.production 配置
cp ../.env.production .env
```

### 步骤5：启动服务

```bash
pm2 start dist/index.js --name word-helper
pm2 save
pm2 startup
```

---

## 环境配置说明

### 配置文件优先级

系统会根据 `NODE_ENV` 自动选择配置文件：

| 环境 | NODE_ENV | 配置文件 | 用途 |
|------|----------|----------|------|
| 开发环境 | `development` | `.env` | 本地开发调试 |
| 测试环境 | `local` | `.env.local` | 测试环境验证 |
| 生产环境 | `production` | `.env.production` | 线上正式环境 |

### 配置文件加载顺序

1. `.env.production` - 生产环境
2. `.env.local` - 本地测试环境  
3. `.env` - 开发环境（默认）

### 配置项说明

| 配置项 | 说明 | 示例值 |
|--------|------|--------|
| `DATABASE_URL` | PostgreSQL 数据库连接 URL | `postgresql://user:pass@host:5432/db` |
| `PORT` | 服务端口 | 3000 |
| `NODE_ENV` | 环境标识 | `development/production` |
| `GITHUB_CLIENT_ID` | GitHub OAuth Client ID | - |
| `GITHUB_CLIENT_SECRET` | GitHub OAuth Client Secret | - |
| `SMTP_HOST` | SMTP 邮件服务器 | `smtp.qq.com` |
| `SMTP_PORT` | SMTP 端口 | 587 |
| `SMTP_USER` | 邮件账号 | `xxx@qq.com` |
| `SMTP_PASS` | 邮件授权码 | - |
| `SMTP_FROM` | 发件人邮箱 | `noreply@word-helper.com` |
| `JWT_SECRET` | JWT 密钥 | 随机字符串 |

### 切换环境

```bash
# 开发环境
NODE_ENV=development npm start

# 生产环境
NODE_ENV=production npm start
```

---

## GitHub OAuth 配置

### 第一步：创建 GitHub OAuth 应用

1. 登录 GitHub，进入 [GitHub Developer Settings](https://github.com/settings/developers)
2. 点击 **"OAuth Apps"** > **"New OAuth App"**

### 第二步：填写应用信息

| 字段 | 值 |
|------|------|
| **Application name** | Word Helper |
| **Homepage URL** | `http://你的服务器IP:3000` |
| **Authorization callback URL** | `http://你的服务器IP:3000/api/auth/github/callback` |

### 第三步：获取凭证

创建成功后，复制：
- **Client ID** - 公开标识符
- **Client Secret** - 点击 "Generate a new client secret" 生成

### 第四步：配置到环境文件

编辑 `.env.production` 文件：

```env
GITHUB_CLIENT_ID=你的ClientID
GITHUB_CLIENT_SECRET=你的ClientSecret
```

---

## SMTP 邮件服务配置

### QQ 邮箱配置步骤

1. **登录 QQ 邮箱** → **设置** → **账户**
2. 找到 **POP3/IMAP/SMTP/Exchange/CardDAV/CalDAV 服务**
3. 开启 **POP3/SMTP 服务** 和 **IMAP/SMTP 服务**
4. 点击 **"生成授权码"**，按提示完成验证
5. 复制生成的授权码

### 配置到环境文件

编辑 `.env.production` 文件：

```env
SMTP_HOST=smtp.qq.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=1933942520@qq.com
SMTP_PASS=lgabmbfmolhpcaeb
SMTP_FROM=1933942520@qq.com
```

### 其他邮箱配置参考

| 邮箱 | SMTP 服务器 | 端口 | SSL |
|------|-------------|------|-----|
| QQ 邮箱 | smtp.qq.com | 587 | false |
| 163 邮箱 | smtp.163.com | 587 | false |
| Gmail | smtp.gmail.com | 587 | true |

---

## 常用维护命令

### 查看状态

```bash
# Docker 方式
docker-compose ps
docker-compose logs -f

# pm2 方式
pm2 status
pm2 logs word-helper
```

### 停止服务

```bash
# Docker 方式
docker-compose down

# pm2 方式
pm2 stop word-helper
```

### 重启服务

```bash
# Docker 方式
docker-compose restart

# pm2 方式
pm2 restart word-helper
```

### 更新代码

```bash
cd /opt/word-helper
git pull origin main

# Docker 方式
docker-compose down
docker-compose up -d --build

# pm2 方式
cd backend
npm install --registry=https://registry.npmmirror.com --legacy-peer-deps
npm run build
pm2 restart word-helper
```

---

## 常见问题解决

### 问题1：SSH 连接超时

**解决方法**：
1. 检查云安全组是否开放 22 端口
2. 检查服务器防火墙
3. 确认 IP 地址和密码正确

### 问题2：Docker 无法拉取镜像

**解决方法**：使用方案1的一键部署脚本，自动切换到离线模式或 Gitee

### 问题3：数据库连接失败

**解决方法**：
```bash
cat /opt/word-helper/.env.production
```
检查 `DATABASE_URL` 是否正确

### 问题4：端口 3000 被占用

**解决方法**：
```bash
# 查看占用进程
lsof -i :3000
# 或
netstat -tulpn | grep 3000

# 结束占用进程
kill -9 <进程ID>
```

### 问题5：GitHub 登录失败

**解决方法**：
1. 检查 `GITHUB_CLIENT_ID` 和 `GITHUB_CLIENT_SECRET` 是否正确
2. 检查 OAuth callback URL 是否匹配

### 问题6：邮件发送失败

**解决方法**：
1. 检查 SMTP 配置是否正确
2. 确认授权码是否正确（不是邮箱密码）
3. 检查服务器防火墙是否开放 587 端口

---

## 🎉 恭喜！

部署成功后访问：`http://你的服务器IP:3000`

---

*文档版本*: 9.1.0  
*最后更新*: 2026-05-30