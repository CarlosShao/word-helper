# Word Helper 项目 - 超详细部署手册

> **版本**: 8.0.0（国内服务器专用版）  
> **更新日期**: 2026-05-24  
> **适用版本**: Word Helper v1.0+  
> **目标读者**: Linux 初学者、新手开发者

---

## 📋 目录

1. [必读！开始之前](#必读开始之前)
2. [方案选择指南](#方案选择指南)
3. [方案1：离线 Docker 部署（推荐，你想要的方案）](#方案1离线-docker-部署推荐你想要的方案)
4. [方案2：直接部署（不用 Docker，最简单）](#方案2直接部署不用-docker最简单)
5. [方案3：传统 Docker 部署（需要网络能访问国外镜像）](#方案3传统-docker-部署需要网络能访问国外镜像)
6. [常用维护命令](#常用维护命令)
7. [常见问题解决](#常见问题解决)
8. [一键启动脚本](#一键启动脚本)

---

## 必读！开始之前

### 你需要准备什么

| 项目 | 说明 | 你有了吗？ |
|------|------|-----------|
| 阿里云服务器 | 已经购买 | ✅ |
| 公网 IP | 例如：182.92.108.240 | ✅ |
| root 密码 | 例如：!henji2168Carlos | ✅ |
| 安全组开放端口 | 22, 3000 | ✅ |

### 开放安全组端口（重要！）

如果还没开放，请在阿里云控制台操作：

1. 登录阿里云控制台 → ECS 实例 → 安全组
2. 点击「配置规则」→「入方向」→「添加安全组规则」
3. 添加以下规则：

| 端口 | 用途 | 协议 | 授权对象 |
|------|------|------|---------|
| 22 | SSH 连接 | TCP | 0.0.0.0/0 |
| 3000 | 项目网站 | TCP | 0.0.0.0/0 |
| 5432 | 数据库（可选） | TCP | 0.0.0.0/0 |

---

## 方案选择指南

选择最适合你的部署方案：

| 方案 | 优点 | 缺点 | 难度 |
|------|------|------|------|
| **方案1：离线 Docker 部署** | ✅ 用 Docker，一次配置长期使用<br>✅ 环境隔离，干净<br>✅ 你想要的方案 | ⚠️ 需要本地先下载镜像<br>⚠️ 需要传到服务器 | ⭐⭐⭐ |
| **方案2：直接部署** | ✅ 最简单，零网络问题<br>✅ 国内服务器完美兼容 | ⚠️ 需要手动配置环境<br>⚠️ 没有容器隔离 | ⭐⭐ |
| **方案3：传统 Docker** | ✅ 最简单（如果网络好） | ⚠️ 国内服务器无法访问 Docker Hub<br>⚠️ 需要代理或镜像源 | ⭐⭐⭐⭐ |

---

---

## 方案1：离线 Docker 部署（推荐，你想要的方案）

### 适用情况
✅ 你想要用 Docker  
✅ 国内服务器无法访问 Docker Hub  
✅ 你有一台能上网的本地电脑

---

### 步骤1：在本地电脑下载 Docker 镜像

首先，你的本地电脑需要先安装 Docker：

**Windows 用户**：下载 [Docker Desktop](https://www.docker.com/products/docker-desktop/)

**Mac 用户**：下载 [Docker Desktop for Mac](https://www.docker.com/products/docker-desktop/)

**Linux 用户**：用系统包管理器安装

安装好 Docker 后，执行：

```bash
# 1. 在你本地电脑上下载基础镜像
docker pull node:20-alpine

# 2. 导出镜像
docker save node:20-alpine > node-20-alpine.tar

# 3. 压缩（可选，减小文件大小）
gzip node-20-alpine.tar

# 现在你会得到一个 node-20-alpine.tar.gz 文件，约 100MB 左右
```

---

### 步骤2：把镜像传到服务器

#### 方法1：用 FileZilla（推荐，有界面）
1. 下载 FileZilla：https://filezilla-project.org/
2. 连接服务器：
   - 主机：182.92.108.240
   - 用户名：root
   - 密码：!henji2168Carlos
   - 端口：22
3. 把 `node-20-alpine.tar.gz` 拖到服务器的 `/opt/` 目录

#### 方法2：用 SCP 命令（高级用户）
```bash
# 在你本地电脑的终端执行
scp node-20-alpine.tar.gz root@182.92.108.240:/opt/
```

---

### 步骤3：在服务器上准备环境

连接服务器后执行：

```bash
# 1. 检查系统
cat /etc/os-release

# 2. CentOS/Alibaba Cloud Linux 系统：
yum install -y git docker

# Ubuntu/Debian 系统：
apt install -y git docker.io docker-compose

# 3. 启动 Docker
systemctl start docker
systemctl enable docker

# 4. 安装 Docker Compose
curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
chmod +x /usr/local/bin/docker-compose
docker-compose --version
```

---

### 步骤4：导入镜像并启动

```bash
# 1. 进入目录
cd /opt

# 2. 解压
gunzip node-20-alpine.tar.gz

# 3. 导入镜像
docker load < node-20-alpine.tar

# 4. 检查镜像
docker images

# 5. 克隆项目
git clone https://github.com/CarlosShao/word-helper.git
cd word-helper

# 6. 配置环境变量（使用远程数据库，避免本地数据库问题）
cat > .env << 'EOF'
DATABASE_URL=postgresql://postgres.gqtsxcypwgtczlugkqsb:!henji2168Carlos@aws-1-ap-southeast-2.pooler.supabase.com:5432/postgres
PGSSLMODE=require
PORT=3000
EOF

# 7. 构建并启动项目
docker-compose up -d --build

# 8. 查看状态
docker-compose ps

# 9. 查看日志
docker-compose logs -f
```

---

### 步骤5：访问网站

打开浏览器访问：
```
http://182.92.108.240:3000
```

---

---

## 方案2：直接部署（不用 Docker，最简单）

### 适用情况
✅ 不想折腾 Docker  
✅ 国内服务器完美兼容  
✅ 最简单，快速上手

---

### 步骤1：检查系统

```bash
cat /etc/os-release
```

---

### 步骤2：CentOS/Alibaba Cloud Linux 部署

```bash
# 1. 更新系统
yum update -y

# 2. 安装 Node.js（国内源）
curl -fsSL https://rpm.nodesource.com/setup_20.x | sudo bash -
sudo yum install -y nodejs

# 3. 检查 Node.js
node --version
npm --version

# 4. 克隆项目
cd /opt
git clone https://github.com/CarlosShao/word-helper.git
cd word-helper

# 5. 安装后端依赖（国内源）
cd /opt/word-helper/backend
npm install --registry=https://registry.npmmirror.com --legacy-peer-deps

# 6. 构建后端
npm run build

# 7. 配置环境变量
cat > .env << 'EOF'
DATABASE_URL=postgresql://postgres.gqtsxcypwgtczlugkqsb:!henji2168Carlos@aws-1-ap-southeast-2.pooler.supabase.com:5432/postgres
PGSSLMODE=require
PORT=3000
EOF

# 8. 启动服务
node dist/index.js
```

#### 让服务在后台运行（用 pm2）

```bash
# 安装 pm2
npm install -g pm2

# 启动服务
cd /opt/word-helper/backend
pm2 start dist/index.js --name word-helper

# 查看状态
pm2 status

# 查看日志
pm2 logs word-helper

# 设置开机自启
pm2 startup
pm2 save
```

---

### 步骤3：Ubuntu/Debian 部署

```bash
# 1. 更新系统
apt update && apt upgrade -y

# 2. 安装 Node.js
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo bash -
sudo apt install -y nodejs

# 3. 克隆项目
cd /opt
git clone https://github.com/CarlosShao/word-helper.git
cd word-helper

# 4. 安装依赖
cd /opt/word-helper/backend
npm install --registry=https://registry.npmmirror.com --legacy-peer-deps

# 5. 构建
npm run build

# 6. 配置环境变量
cat > .env << 'EOF'
DATABASE_URL=postgresql://postgres.gqtsxcypwgtczlugkqsb:!henji2168Carlos@aws-1-ap-southeast-2.pooler.supabase.com:5432/postgres
PGSSLMODE=require
PORT=3000
EOF

# 7. 安装 pm2
npm install -g pm2

# 8. 启动服务
pm2 start dist/index.js --name word-helper
pm2 save
pm2 startup
```

---

### 步骤4：访问网站

```
http://182.92.108.240:3000
```

---

---

## 方案3：传统 Docker 部署（需要网络能访问国外镜像）

### 适用情况
✅ 网络能访问 Docker Hub  
✅ 或者你有代理

---

### 步骤1：配置 Docker 国内镜像源

```bash
mkdir -p /etc/docker

cat > /etc/docker/daemon.json << 'EOF'
{
  "registry-mirrors": [
    "https://docker.m.daocloud.io",
    "https://docker.1panel.live",
    "https://docker.1ms.run"
  ]
}
EOF

systemctl daemon-reload
systemctl restart docker

docker pull hello-world
```

---

### 步骤2：部署（如果镜像源能用）

```bash
# 1. 安装 Docker 和 Docker Compose
# CentOS/Alibaba Cloud Linux:
yum install -y git docker
curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
chmod +x /usr/local/bin/docker-compose

# Ubuntu/Debian:
apt install -y git docker.io docker-compose

# 2. 启动 Docker
systemctl start docker
systemctl enable docker

# 3. 克隆项目
cd /opt
git clone https://github.com/CarlosShao/word-helper.git
cd word-helper

# 4. 配置环境变量
cat > .env << 'EOF'
DATABASE_URL=postgresql://postgres.gqtsxcypwgtczlugkqsb:!henji2168Carlos@aws-1-ap-southeast-2.pooler.supabase.com:5432/postgres
PGSSLMODE=require
PORT=3000
EOF

# 5. 构建并启动
docker-compose up -d --build
```

---

---

## 常用维护命令

### 查看状态

**Docker 方式：**
```bash
docker-compose ps
docker-compose logs -f
```

**直接部署（pm2）方式：**
```bash
pm2 status
pm2 logs word-helper
```

### 停止项目

**Docker 方式：**
```bash
docker-compose down
```

**直接部署方式：**
```bash
pm2 stop word-helper
```

### 重启项目

**Docker 方式：**
```bash
docker-compose restart
```

**直接部署方式：**
```bash
pm2 restart word-helper
```

### 更新代码

```bash
cd /opt/word-helper
git pull origin main

# Docker 方式：
docker-compose down
docker-compose up -d --build

# 直接部署方式：
cd backend
npm install --registry=https://registry.npmmirror.com --legacy-peer-deps
npm run build
pm2 restart word-helper
```

---

## 常见问题解决

### 问题1：SSH 连接超时

**解决方法**：
1. 检查阿里云安全组是否开放 22 端口
2. 检查服务器防火墙
3. 确认 IP 地址和密码正确

### 问题2：Docker 无法拉取镜像

**解决方法**：
- 用方案1（离线 Docker 部署）
- 或用方案2（直接部署）

### 问题3：数据库连接失败

**解决方法**：
```bash
# 检查 .env 文件配置
cat /opt/word-helper/.env
cat /opt/word-helper/backend/.env
```

### 问题4：端口 3000 被占用

**解决方法**：
```bash
# 查看占用端口的进程
lsof -i :3000
# 或
netstat -tulpn | grep 3000

# 结束占用进程
kill -9 <进程ID>
```

### 问题5：页面显示错误

**解决方法**：
```bash
# 查看日志
# Docker 方式：
docker-compose logs -f

# 直接部署方式：
pm2 logs word-helper
```

---

## 一键启动脚本

### Docker 方式的一键启动脚本

```bash
cd /opt/word-helper

cat > start.sh << 'EOF'
#!/bin/bash
# Word Helper 一键启动脚本
# Docker 方式

echo "========================================="
echo "  Word Helper 启动脚本"
echo "========================================="
echo ""

if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装"
    exit 1
fi

if ! command -v docker-compose &> /dev/null; then
    echo "❌ Docker Compose 未安装"
    exit 1
fi

if [ ! -f .env ]; then
    echo "📝 创建 .env 文件..."
    cat > .env << 'ENVVAR'
DATABASE_URL=postgresql://postgres.gqtsxcypwgtczlugkqsb:!henji2168Carlos@aws-1-ap-southeast-2.pooler.supabase.com:5432/postgres
PGSSLMODE=require
PORT=3000
ENVVAR
fi

echo "🚀 启动 Docker 容器..."
docker-compose up -d

sleep 10

echo ""
echo "========================================="
echo "  ✅ 部署成功！"
echo "========================================="
echo ""
echo "📱 访问地址："
echo "   http://$(hostname -I | awk '{print $1}'):3000"
echo ""
EOF

chmod +x start.sh
```

### 直接部署方式的一键启动脚本

```bash
cd /opt/word-helper/backend

cat > start.sh << 'EOF'
#!/bin/bash
# Word Helper 一键启动脚本
# 直接部署方式

echo "========================================="
echo "  Word Helper 启动脚本"
echo "========================================="
echo ""

if ! command -v node &> /dev/null; then
    echo "❌ Node.js 未安装"
    exit 1
fi

if ! command -v pm2 &> /dev/null; then
    echo "📦 安装 pm2..."
    npm install -g pm2
fi

if [ ! -f .env ]; then
    echo "📝 创建 .env 文件..."
    cat > .env << 'ENVVAR'
DATABASE_URL=postgresql://postgres.gqtsxcypwgtczlugkqsb:!henji2168Carlos@aws-1-ap-southeast-2.pooler.supabase.com:5432/postgres
PGSSLMODE=require
PORT=3000
ENVVAR
fi

echo "🚀 启动服务..."
pm2 start dist/index.js --name word-helper
pm2 save

echo ""
echo "========================================="
echo "  ✅ 部署成功！"
echo "========================================="
echo ""
echo "📱 访问地址："
echo "   http://$(hostname -I | awk '{print $1}'):3000"
echo ""
EOF

chmod +x start.sh
```

---

## 🎉 恭喜！

如果你看到这个说明，说明文档已经更新完成！

**推荐方案**：
1. 如果你想**用 Docker** → 用方案1（离线 Docker 部署）
2. 如果你想**最快最简单** → 用方案2（直接部署）

**访问地址**：`http://182.92.108.240:3000`

---

### 遇到问题？

1. 查看日志
2. 参考「常见问题解决」章节
3. 重新检查一遍步骤

---

*文档版本*: 8.0.0  
*最后更新*: 2026-05-24
