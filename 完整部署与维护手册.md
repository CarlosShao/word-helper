# Word Helper 项目 - 超详细部署手册

> **版本**: 7.0.0（CentOS + Ubuntu 双版本）  
> **更新日期**: 2026-05-24  
> **适用版本**: Word Helper v1.0+  
> **目标读者**: Linux 初学者、新手开发者  

---

## 📋 目录

1. [必读！开始之前](#必读开始之前)
2. [第0步：检查你的服务器系统](#第0步检查你的服务器系统)
3. [第1步：连接服务器](#第1步连接服务器)
4. [方案A：CentOS/Alibaba Cloud Linux 部署](#方案acentosalibaba-cloud-linux-部署)
5. [方案B：Ubuntu/Debian 部署](#方案bubuntudebian-部署)
6. [第7步：访问网站](#第7步访问网站)
7. [常用维护命令](#常用维护命令)
8. [常见问题解决](#常见问题解决)
9. [一键启动脚本](#一键启动脚本)

---

## 必读！开始之前

### 你需要准备什么

| 项目 | 说明 | 你有了吗？ |
|------|------|-----------|
| 阿里云服务器 | 已经购买 | ✅ |
| 公网 IP | 例如：182.92.108.240 | ✅ |
| root 密码 | 例如：!henji2168Carlos | ✅ |
| 安全组开放端口 | 22, 3000 | ✅ |

### 开放安全组端口（重要！）

如果还没开放，请在阿里云控制台操作：

1. 登录阿里云控制台 → ECS 实例 → 安全组
2. 点击「配置规则」→「入方向」→「添加安全组规则」
3. 添加以下规则：

| 端口 | 用途 | 协议 | 授权对象 |
|------|------|------|---------|
| 22 | SSH 连接 | TCP | 0.0.0.0/0 |
| 3000 | 项目网站 | TCP | 0.0.0.0/0 |
| 5432 | 数据库（可选） | TCP | 0.0.0.0/0 |

---

## 第0步：检查你的服务器系统

在服务器终端输入：

```bash
cat /etc/os-release
```

### 情况1：CentOS/Alibaba Cloud Linux

如果看到类似：

```
NAME="CentOS Linux"
或
NAME="Alibaba Cloud Linux"
```

请用「方案A」部署。

### 情况2：Ubuntu/Debian

如果看到类似：

```
NAME="Ubuntu"
或
NAME="Debian"
```

请用「方案B」部署。

---

## 第1步：连接服务器

### Windows 用户

#### 方法1：使用 CMD（简单）

1. 按 `Win + R`，输入 `cmd`，回车
2. 在黑色窗口中输入：

```bash
ssh root@182.92.108.240
```

3. 提示输入密码时，输入：`!henji2168Carlos`（输入时不会显示，输入完直接回车）

#### 方法2：使用 SSH 工具（推荐）

下载以下软件之一，更方便：
- [Xshell](https://www.xshell.com/zh-cn/xshell/)（免费）
- [Putty](https://www.putty.org/)（免费）
- [Termius](https://termius.com/)（免费）

### Mac 用户

1. 打开「终端」（Launchpad → 其他 → 终端）
2. 输入：

```bash
ssh root@182.92.108.240
```

### 连接成功！

看到类似以下画面，说明连接成功：

```
Welcome to Alibaba Cloud Elastic Compute Service !
root@your-server:~#
```

---

## 方案A：CentOS/Alibaba Cloud Linux 部署

### 第2步：安装 Docker Compose（必须先装！）

```bash
# 下载 Docker Compose
curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose

# 赋予执行权限
chmod +x /usr/local/bin/docker-compose

# 检查是否安装成功
docker-compose --version
```

### 第3步：一键部署（复制下面所有命令，粘贴到终端）

```bash
echo "========================================="
echo "  Word Helper 一键部署脚本 (CentOS)"
echo "========================================="
echo ""

# 步骤1：更新系统
echo "📦 步骤1/7：更新系统..."
yum update -y

# 步骤2：安装必要软件
echo "📦 步骤2/7：安装必要软件..."
yum install -y git docker postgresql-server

# 步骤3：启动 Docker
echo "🐳 步骤3/7：启动 Docker..."
systemctl start docker
systemctl enable docker

# 步骤4：配置数据库
echo "🗄️ 步骤4/7：配置数据库..."
postgresql-setup --initdb
systemctl start postgresql
systemctl enable postgresql

# 步骤5：设置数据库密码
sudo -u postgres psql -c "ALTER USER postgres PASSWORD '!henji2168Carlos';"
sudo -u postgres psql -c "CREATE DATABASE wordhelper;"

# 步骤6：下载项目
echo "📦 步骤5/7：下载项目代码..."
cd /opt
git clone https://github.com/CarlosShao/word-helper.git
cd word-helper

# 步骤7：配置环境变量
echo "⚙️ 步骤6/7：配置环境变量..."
cat > .env << 'EOF'
DATABASE_URL=postgresql://postgres:!henji2168Carlos@localhost:5432/wordhelper
PGSSLMODE=disable
PORT=3000
EOF

# 步骤8：启动项目
echo "🚀 步骤7/7：启动项目..."
docker-compose up -d

# 等待启动完成
echo "⏳ 等待服务启动（10秒）..."
sleep 10

# 完成！
echo ""
echo "========================================="
echo "  ✅ 部署成功！"
echo "========================================="
echo ""
echo "📱 访问地址："
echo "   前端：http://$(hostname -I | awk '{print $1}'):3000"
echo "   后端：http://$(hostname -I | awk '{print $1}'):3000/api"
echo ""
echo "🔧 常用命令："
echo "   查看状态：docker-compose ps"
echo "   查看日志：docker-compose logs -f"
echo "   停止项目：docker-compose down"
echo "   重启项目：docker-compose restart"
echo ""
```

---

## 方案B：Ubuntu/Debian 部署

### 第2步：一键部署（复制下面所有命令，粘贴到终端）

```bash
echo "========================================="
echo "  Word Helper 一键部署脚本 (Ubuntu)"
echo "========================================="
echo ""

# 步骤1：更新系统
echo "📦 步骤1/7：更新系统..."
apt update && apt upgrade -y

# 步骤2：安装必要软件
echo "📦 步骤2/7：安装必要软件..."
apt install -y git docker.io docker-compose postgresql postgresql-contrib

# 步骤3：启动 Docker
echo "🐳 步骤3/7：启动 Docker..."
systemctl start docker
systemctl enable docker

# 步骤4：配置数据库
echo "🗄️ 步骤4/7：配置数据库..."
sudo -u postgres psql -c "ALTER USER postgres PASSWORD '!henji2168Carlos';"
sudo -u postgres psql -c "CREATE DATABASE wordhelper;"

# 步骤5：下载项目
echo "📦 步骤5/7：下载项目代码..."
cd /opt
git clone https://github.com/CarlosShao/word-helper.git
cd word-helper

# 步骤6：配置环境变量
echo "⚙️ 步骤6/7：配置环境变量..."
cat > .env << 'EOF'
DATABASE_URL=postgresql://postgres:!henji2168Carlos@localhost:5432/wordhelper
PGSSLMODE=disable
PORT=3000
EOF

# 步骤7：启动项目
echo "🚀 步骤7/7：启动项目..."
docker-compose up -d

# 等待启动完成
echo "⏳ 等待服务启动（10秒）..."
sleep 10

# 完成！
echo ""
echo "========================================="
echo "  ✅ 部署成功！"
echo "========================================="
echo ""
echo "📱 访问地址："
echo "   前端：http://$(hostname -I | awk '{print $1}'):3000"
echo "   后端：http://$(hostname -I | awk '{print $1}'):3000/api"
echo ""
echo "🔧 常用命令："
echo "   查看状态：docker-compose ps"
echo "   查看日志：docker-compose logs -f"
echo "   停止项目：docker-compose down"
echo "   重启项目：docker-compose restart"
echo ""
```

---

## 第7步：访问网站

### 在浏览器中打开

```
http://182.92.108.240:3000
```

（替换成你的服务器 IP）

### 如果无法访问，检查以下几点：

1. 确认阿里云安全组开放了 3000 端口
2. 确认服务器防火墙放行 3000 端口：
   ```bash
   # CentOS 系统
   firewall-cmd --permanent --add-port=3000/tcp
   firewall-cmd --reload

   # Ubuntu 系统
   ufw allow 3000
   ```
3. 确认项目正在运行：
   ```bash
   docker-compose ps
   ```

---

## 常用维护命令

### 查看状态

```bash
# 查看运行状态
docker-compose ps

# 查看日志
docker-compose logs -f
```

### 停止项目

```bash
docker-compose down
```

### 启动项目

```bash
docker-compose up -d
```

### 重启项目

```bash
docker-compose restart
```

### 更新代码

```bash
# 1. 进入项目目录
cd /opt/word-helper

# 2. 拉取最新代码
git pull origin main

# 3. 重启项目
docker-compose down
docker-compose up -d
```

### 查看端口占用

```bash
# 查看 3000 端口是否被占用
lsof -i :3000
# 或
netstat -tulpn | grep 3000
```

### 数据库备份

```bash
# 备份数据库
sudo -u postgres pg_dump wordhelper > backup_$(date +%Y%m%d).sql
```

### 数据库恢复

```bash
# 恢复数据库
sudo -u postgres psql wordhelper < backup_20260524.sql
```

---

## 常见问题解决

### 问题1：SSH 连接超时

**解决方法**：
1. 检查阿里云安全组是否开放 22 端口
2. 检查服务器防火墙
3. 确认 IP 地址和密码正确

### 问题2：Docker 启动失败

**解决方法**：
```bash
# 检查 Docker 服务状态
systemctl status docker

# 如果没运行，启动 Docker
systemctl start docker
systemctl enable docker
```

### 问题3：数据库连接失败

**解决方法**：
```bash
# 检查 PostgreSQL 服务
systemctl status postgresql

# 检查数据库是否存在
sudo -u postgres psql -l

# 检查 .env 文件配置
cat /opt/word-helper/.env
```

### 问题4：端口 3000 被占用

**解决方法**：
```bash
# 查看占用端口的进程
lsof -i :3000

# 结束占用进程
kill -9 <进程ID>
```

### 问题5：页面显示错误

**解决方法**：
```bash
# 查看项目日志
docker-compose logs -f

# 重新构建并启动
docker-compose down
docker-compose up -d --build
```

---

## 一键启动脚本

### 创建 start.sh

在项目目录中创建一键启动脚本：

```bash
cd /opt/word-helper

cat > start.sh << 'EOF'
#!/bin/bash
# Word Helper 一键启动脚本
# 新手友好版

echo "========================================="
echo "  Word Helper 启动脚本"
echo "========================================="
echo ""

# 检查 Docker 是否安装
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装，请先运行安装脚本"
    exit 1
fi

# 检查 Docker Compose 是否安装
if ! command -v docker-compose &> /dev/null; then
    echo "❌ Docker Compose 未安装，请先运行安装脚本"
    exit 1
fi

# 检查 .env 文件是否存在
if [ ! -f .env ]; then
    echo "📝 创建 .env 文件..."
    cat > .env << 'ENVVAR'
DATABASE_URL=postgresql://postgres:!henji2168Carlos@localhost:5432/wordhelper
PGSSLMODE=disable
PORT=3000
ENVVAR
fi

echo "🚀 启动 Docker 容器..."
docker-compose up -d

# 等待启动完成
echo "⏳ 等待服务启动（10秒）..."
sleep 10

echo ""
echo "========================================="
echo "  ✅ 部署成功！"
echo "========================================="
echo ""
echo "📱 访问地址："
echo "   前端：http://$(hostname -I | awk '{print $1}'):3000"
echo "   后端：http://$(hostname -I | awk '{print $1}'):3000/api"
echo ""
echo "🔧 常用命令："
echo "   查看状态：docker-compose ps"
echo "   查看日志：docker-compose logs -f"
echo "   停止项目：docker-compose down"
echo "   重启项目：docker-compose restart"
echo ""
EOF

# 赋予执行权限
chmod +x start.sh
```

### 使用方法

```bash
# 1. 赋予执行权限（第一次需要）
chmod +x start.sh

# 2. 启动项目
./start.sh
```

---

## 🎉 恭喜！

如果你看到这个说明，说明项目已经成功部署了！

访问地址：`http://182.92.108.240:3000`

---

### 遇到问题？

1. 查看日志：`docker-compose logs -f`
2. 参考「常见问题解决」章节
3. 按文档步骤重新检查一遍

---

*文档版本*: 7.0.0  
*最后更新*: 2026-05-24
