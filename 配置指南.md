# Word Helper 配置指南

> **版本**: 1.0  
> **更新日期**: 2026-05-29  
> **适用版本**: Word Helper v1.0+

---

## 📋 目录

1. [环境配置文件说明](#环境配置文件说明)
2. [GitHub OAuth 配置](#github-oauth-配置)
3. [SMTP 邮件服务配置](#smtp-邮件服务配置)
4. [环境变量自动识别](#环境变量自动识别)

---

## 环境配置文件说明

### 配置文件优先级

Word Helper 支持多环境配置，系统会自动根据环境变量 `NODE_ENV` 选择对应的配置文件：

| 环境 | NODE_ENV | 配置文件 | 用途 |
|------|----------|----------|------|
| 开发环境 | `development` | `.env` | 本地开发调试 |
| 测试环境 | `local` | `.env.local` | 测试环境验证 |
| 生产环境 | `production` | `.env.production` | 线上正式环境 |

### 配置文件加载顺序

1. `.env.production` - 生产环境（NODE_ENV=production）
2. `.env.local` - 本地测试环境（NODE_ENV=local）
3. `.env` - 开发环境（默认）

### 配置项说明

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| `DATABASE_URL` | PostgreSQL 数据库连接 URL | - |
| `PORT` | 服务端口 | 3000 |
| `NODE_ENV` | 环境标识 | development |
| `GITHUB_CLIENT_ID` | GitHub OAuth Client ID | - |
| `GITHUB_CLIENT_SECRET` | GitHub OAuth Client Secret | - |
| `SMTP_HOST` | SMTP 邮件服务器地址 | smtp.qq.com |
| `SMTP_PORT` | SMTP 端口 | 587 |
| `SMTP_SECURE` | 是否启用 SSL | false |
| `SMTP_USER` | 邮件账号 | - |
| `SMTP_PASS` | 邮件密码/授权码 | - |
| `SMTP_FROM` | 发件人邮箱 | noreply@word-helper.com |
| `JWT_SECRET` | JWT 密钥 | - |

---

## GitHub OAuth 配置

### 第一步：创建 GitHub OAuth 应用

1. 登录 GitHub，进入 [GitHub Developer Settings](https://github.com/settings/developers)

2. 点击 **"OAuth Apps"** > **"New OAuth App"**

3. 填写应用信息：

| 字段 | 值 |
|------|------|
| **Application name** | Word Helper |
| **Homepage URL** | `http://your-domain.com:3000` |
| **Authorization callback URL** | `http://your-domain.com:3000/api/auth/github/callback` |

4. 点击 **"Register application"**

### 第二步：获取 Client ID 和 Client Secret

创建成功后，你会看到：
- **Client ID** - 公开的标识符
- **Client Secret** - 保密的密钥（点击 "Generate a new client secret" 生成）

### 第三步：配置到环境文件

将获取到的值填入 `.env` 文件：

```env
GITHUB_CLIENT_ID=your_client_id_here
GITHUB_CLIENT_SECRET=your_client_secret_here
```

### 第四步：测试 GitHub 登录

启动应用后，访问登录页面，点击 "GitHub Login" 按钮测试登录流程。

---

## SMTP 邮件服务配置

以 QQ 邮箱为例，其他邮箱配置类似。

### 第一步：开启 QQ 邮箱 SMTP

1. 登录 QQ 邮箱，进入 **设置** > **账户**

2. 找到 **POP3/IMAP/SMTP/Exchange/CardDAV/CalDAV 服务**

3. 开启以下选项：
   - ✅ POP3/SMTP 服务
   - ✅ IMAP/SMTP 服务

### 第二步：获取授权码

1. 在同一页面，找到 **"授权码"** 选项

2. 点击 **"生成授权码"**

3. 按提示完成验证（短信验证等）

4. 复制生成的授权码

### 第三步：配置到环境文件

```env
SMTP_HOST=smtp.qq.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_qq_email@qq.com
SMTP_PASS=your_authorization_code
SMTP_FROM=noreply@word-helper.com
```

### 其他邮箱配置参考

| 邮箱 | SMTP 服务器 | 端口 | SSL |
|------|-------------|------|-----|
| QQ 邮箱 | smtp.qq.com | 587 | false |
| 163 邮箱 | smtp.163.com | 587 | false |
| Gmail | smtp.gmail.com | 587 | true |
| Outlook | smtp.office365.com | 587 | true |

### 测试邮件功能

启动应用后，使用 "忘记密码" 功能测试邮件发送。

---

## 环境变量自动识别

### 方式一：通过 NODE_ENV 环境变量

```bash
# 开发环境
NODE_ENV=development npm start

# 测试环境
NODE_ENV=local npm start

# 生产环境
NODE_ENV=production npm start
```

### 方式二：通过启动脚本设置

在 `start.sh` 或 `start.bat` 中设置：

```bash
export NODE_ENV=production
npm start
```

### 方式三：Docker Compose 设置

在 `docker-compose.yml` 中设置：

```yaml
environment:
  - NODE_ENV=production
```

---

## 📝 完整配置示例

### 开发环境 `.env`

```env
# 数据库
DATABASE_URL=postgresql://user:password@localhost:5432/wordhelper

# 服务器
PORT=3000
NODE_ENV=development

# GitHub OAuth
GITHUB_CLIENT_ID=dev_github_client_id
GITHUB_CLIENT_SECRET=dev_github_client_secret

# SMTP
SMTP_HOST=smtp.qq.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=dev@qq.com
SMTP_PASS=dev_authorization_code
SMTP_FROM=noreply@word-helper.com

# 安全
JWT_SECRET=dev_secret_key
```

### 生产环境 `.env.production`

```env
# 数据库
DATABASE_URL=postgresql://prod_user:prod_password@prod-db:5432/wordhelper

# 服务器
PORT=3000
NODE_ENV=production

# GitHub OAuth
GITHUB_CLIENT_ID=prod_github_client_id
GITHUB_CLIENT_SECRET=prod_github_client_secret

# SMTP
SMTP_HOST=smtp.qq.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=prod@qq.com
SMTP_PASS=prod_authorization_code
SMTP_FROM=noreply@word-helper.com

# 安全
JWT_SECRET=very_long_and_secure_production_secret_key
```

---

## 🔧 常见问题

### Q1: GitHub 登录提示 "Client ID not configured"

**解决方法**：检查 `.env` 文件中是否正确配置了 `GITHUB_CLIENT_ID` 和 `GITHUB_CLIENT_SECRET`

### Q2: 邮件发送失败

**解决方法**：
1. 检查 SMTP 配置是否正确
2. 确认授权码是否正确（不是邮箱密码）
3. 检查服务器防火墙是否开放 587 端口

### Q3: 环境配置不生效

**解决方法**：
1. 检查 `NODE_ENV` 是否正确设置
2. 确认配置文件存在且格式正确
3. 重启应用使配置生效

---

> 📌 **重要提示**：请妥善保管 `.env` 文件，不要将其提交到版本控制系统中！