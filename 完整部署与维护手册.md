# Word Helper 项目 - 完整部署与维护手册

> **版本**: 2.0.0  
> **更新日期**: 2026-05-23  
> **适用版本**: Word Helper v1.0+  
> **推荐部署组合**: Render（后端）+ Neon（数据库）+ Vercel（前端）

---

## 📋 目录

1. [项目概述](#项目概述)
2. [部署架构](#部署架构)
3. [前置准备](#前置准备)
4. [第一步：部署 Neon PostgreSQL 数据库](#第一步部署-neon-postgresql-数据库)
5. [第二步：部署后端到 Render](#第二步部署后端到-render)
6. [第三步：部署前端到 Vercel](#第三步部署前端到-vercel)
7. [环境变量配置](#环境变量配置)
8. [数据库初始化](#数据库初始化)
9. [访问和测试](#访问和测试)
10. [维护指南](#维护指南)
11. [常见问题排查](#常见问题排查)
12. [成本说明](#成本说明)
13. [重要提醒](#重要提醒)

---

## 项目概述

### 什么是 Word Helper？

Word Helper 是一个在线单词学习工具，支持：
- PDF 单词导入
- 智能分类（词根词缀）
- 错题复习
- 随手拼练习模式

### 技术栈

| 组件 | 技术 | 托管平台 | 费用 |
|------|------|----------|------|
| **前端** | Vue 3 + Vite | Vercel | 免费 |
| **后端** | Node.js + Express + TypeScript | Render | 免费 |
| **数据库** | PostgreSQL 17 | Neon | **永久免费** |

### 选择这套方案的原因

1. **Neon 永久免费**：不像 Render PostgreSQL 有 90 天过期限制
2. **访问速度快**：新加坡/亚太节点
3. **稳定可靠**：不休眠（对比 Render 免费版）
4. **零成本**：完全免费，适合个人项目

---

## 部署架构

```
┌─────────────────────────────────────────────────────────────┐
│                        用户浏览器                             │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                    Vercel (前端)                            │
│               https://word-helper.vercel.app               │
│                    Vue 3 + Vite                            │
└────────────────────────┬────────────────────────────────────┘
                         │ API 请求
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                   Render (后端 API)                         │
│              https://word-helper-api.onrender.com          │
│                   Node.js + Express                        │
└────────────────────────┬────────────────────────────────────┘
                         │ PostgreSQL 连接
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                   Neon PostgreSQL                           │
│              永久免费 · 3GB 存储 · 亚太节点                 │
│              ep-cool-breeze-xxxx.us-east-2.aws.neon.tech  │
└─────────────────────────────────────────────────────────────┘
```

---

## 前置准备

### 需要的账号

1. **GitHub 账号** - 代码托管（如果没有）
2. **Neon 账号** - 数据库（永久免费）
3. **Render 账号** - 后端部署（免费）
4. **Vercel 账号** - 前端部署（免费）

### 账号注册链接

| 平台 | 注册地址 | 备注 |
|------|----------|------|
| GitHub | https://github.com | 代码仓库 |
| Neon | https://neon.tech | 数据库（**永久免费**）|
| Render | https://dashboard.render.com | 后端部署 |
| Vercel | https://vercel.com | 前端部署 |

### 代码准备

确保代码已推送到 GitHub：

```bash
cd word-helper
git status
git add .
git commit -m "准备部署"
git push origin main
```

---

## 第一步：部署 Neon PostgreSQL 数据库

> **重要**：Neon 提供永久免费 PostgreSQL 数据库，**没有过期限制**，不用担心数据丢失。

### 1.1 注册 Neon 账号

1. 访问 [https://neon.tech](https://neon.tech)
2. 点击 **Sign Up** 按钮
3. 选择 **GitHub** 登录（最简单）
4. 授权 GitHub 访问

### 1.2 创建数据库项目

1. 登录后，点击 **New Project**

2. 配置项目信息：

| 配置项 | 推荐值 | 说明 |
|--------|--------|------|
| **Project Name** | `word-helper` | 项目名称 |
| **PostgreSQL Version** | `17` | 最新稳定版 |
| **Region** | `US East (Ohio)` | 美东节点（速度较快）|
| **Compute Size** | `0.25 vCPU` | 免费版默认 |

3. 点击 **Create Project**

### 1.3 保存数据库凭证

> ⚠️ **重要**：密码只会显示一次，请立即保存！

创建项目后，会显示连接信息：

```
🎉 Your project is ready!

Connection Details:
- Host: ep-cool-breeze-123456.us-east-2.aws.neon.tech
- Database: neondb
- User: neondb_owner
- Password: npg_xxxxxxxxxxxxxxx

Connection string:
postgresql://neondb_owner:npg_xxxxxxxxxxxxxxx@ep-cool-breeze-123456.pooler.us-east-2.aws.neon.tech/neondb?sslmode=require
```

**保存以下信息**：
- ✅ 连接字符串（Connection string）
- ✅ 数据库密码

### 1.4 了解 Neon 免费版限制

| 项目 | 免费额度 | 说明 |
|------|----------|------|
| **存储空间** | 3 GB | 超出需付费 |
| **分支数据库** | 支持 | 可以创建测试分支 |
| **自动休眠** | ❌ 不会 | 随时可访问 |
| **SSL** | ✅ 必须 | 自动配置 |

### 1.5 配置数据库连接

在 Neon 控制台，你可以看到两种连接字符串：

| 类型 | 用途 | 推荐场景 |
|------|------|----------|
| **Pooled Connection** | 应用连接 | ✅ 用于 Render/Vercel |
| **Direct Connection** | 迁移/管理 | 本地开发用 |

**复制 Pooled Connection**，格式类似：
```
postgresql://用户名:密码@ep-xxx-xxx.pooler.us-east-2.aws.neon.tech/数据库名?sslmode=require
```

---

## 第二步：部署后端到 Render

### 2.1 登录 Render

1. 访问 [https://dashboard.render.com](https://dashboard.render.com)
2. 点击 **Sign In**
3. 选择 **GitHub** 登录

### 2.2 创建 Web 服务

1. 点击 **New** → **Web Service**

2. 配置服务：

| 配置项 | 值 | 说明 |
|--------|-----|------|
| **GitHub** | 选择 `word-helper` 仓库 | 你的代码仓库 |
| **Branch** | `main` | 要部署的分支 |
| **Root Directory** | `backend` | 后端代码目录 |
| **Runtime** | `Node` | 运行环境 |
| **Build Command** | `npm install && npm run build` | 构建命令 |
| **Start Command** | `npm start` | 启动命令 |
| **Plan** | `Free` | 免费计划 |
| **Region** | `Singapore` | 新加坡节点 |

3. 点击 **Create Web Service**

### 2.3 配置环境变量

在服务创建后，进入 **Environment** 标签页，添加以下变量：

| 变量名 | 值 | 说明 |
|--------|-----|------|
| `NODE_ENV` | `production` | 生产环境 |
| `PORT` | `10000` | 端口号 |
| `DATABASE_URL` | `[你的 Neon 连接字符串]` | **粘贴 Neon 的连接字符串** |
| `PGSSLMODE` | `require` | SSL 模式 |

> 💡 **注意**：从 Neon 复制的连接字符串已经包含 `?sslmode=require`，如果 Render 提示重复，可以只填前面的部分。

### 2.4 等待构建完成

构建过程约 3-5 分钟，包括：
1. 安装依赖（npm install）
2. TypeScript 编译（npm run build）
3. 启动服务

构建日志可以在 **Logs** 标签页查看。

### 2.5 验证后端部署

构建完成后，访问：
```
https://word-helper-api.onrender.com/health
```

应该返回：
```json
{
  "status": "ok",
  "timestamp": "2026-05-23T10:00:00.000Z"
}
```

### 2.6 获取后端 API 地址

部署成功后，Render 会提供服务的 URL，例如：
```
https://word-helper-api.onrender.com
```

**保存这个地址**，前端部署时需要用到。

---

## 第三步：部署前端到 Vercel

### 3.1 登录 Vercel

1. 访问 [https://vercel.com](https://vercel.com)
2. 点击 **Sign Up**
3. 选择 **GitHub** 登录

### 3.2 导入项目

1. 点击 **Add New** → **Project**

2. 选择 `word-helper` 仓库

3. Vercel 会自动检测前端配置

### 3.3 配置构建设置

Vercel 自动配置：
- **Framework Preset**: `Vite`
- **Build Command**: `npm run build`
- **Output Directory**: `dist`
- **Install Command**: `npm install`

### 3.4 配置环境变量

在 **Environment Variables** 部分添加：

| 变量名 | 值 | 说明 |
|--------|-----|------|
| `VITE_API_URL` | `https://word-helper-api.onrender.com` | 你的 Render 后端地址 |

> ⚠️ **重要**：这里的地址是第二步获取的后端 API 地址。

### 3.5 部署

1. 点击 **Deploy**
2. 等待构建完成（约 1-2 分钟）
3. 部署成功后，获得前端地址

### 3.6 配置路由重写

由于前端是 SPA（单页应用），需要配置所有路由回退到 `index.html`。

在项目根目录创建 `vercel.json`：

```json
{
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
```

或者在 Vercel Dashboard 中配置：

1. 进入项目设置
2. 点击 **Routes**
3. 添加重写规则：
   - Source: `/(.*)`
   - Destination: `/index.html`

---

## 环境变量配置

### 后端环境变量（Render）

| 变量名 | 示例值 | 说明 |
|--------|--------|------|
| `NODE_ENV` | `production` | 生产环境标识 |
| `PORT` | `10000` | 服务端口 |
| `DATABASE_URL` | `postgresql://user:pass@ep-xxx...neon.tech/neondb?sslmode=require` | 数据库连接 |
| `PGSSLMODE` | `require` | SSL 连接模式 |

### 前端环境变量（Vercel）

| 变量名 | 示例值 | 说明 |
|--------|--------|------|
| `VITE_API_URL` | `https://word-helper-api.onrender.com` | 后端 API 地址 |

### 修改 API 地址

如果后端 API 地址变化，需要更新前端：

1. 修改 `frontend/src/api.ts` 中的 `baseURL`
2. 重新部署前端

---

## 数据库初始化

### 自动初始化

后端服务首次启动时，会自动创建所有必要的表：

```sql
-- 自动创建的表
- words                 -- 单词表
- error_words          -- 错题集
- observation_words    -- 观察室
- import_files         -- 导入文件记录
- word_relations       -- 单词关系
- classification_rules  -- 分类规则
- practice_sessions    -- 练习会话
- settings             -- 设置
- parts_of_speech      -- 词性
- import_error_logs    -- 导入错误日志
```

### 手动初始化（可选）

如果需要手动初始化数据库：

1. 进入 Render 服务页面
2. 点击 **Shell**
3. 执行：
   ```bash
   node dist/index.js
   ```

---

## 访问和测试

### 访问地址

| 服务 | 地址 | 说明 |
|------|------|------|
| **前端** | `https://word-helper.vercel.app` | Vercel 分配的地址 |
| **后端 API** | `https://word-helper-api.onrender.com` | Render 分配的地址 |
| **健康检查** | `https://word-helper-api.onrender.com/health` | API 状态检查 |

### 测试功能

1. **访问前端**：打开前端地址
2. **登录测试**：使用默认账号 `carlos` / `swq`
3. **导入单词**：上传 PDF 测试
4. **检查错题**：添加单词到错题集

---

## 维护指南

### 1. 代码更新

#### 自动部署

推送代码到 GitHub 后，Render 和 Vercel 会自动检测并部署：

```bash
git add .
git commit -m "更新内容"
git push origin main
```

#### 手动部署

在 Render/Vercel Dashboard：
1. 进入服务页面
2. 点击 **Deployments**
3. 选择要部署的版本
4. 点击 **Redeploy**

### 2. 数据库备份

#### 导出数据（使用 pg_dump）

1. 在本地安装 PostgreSQL 客户端
2. 使用 Neon 的外部连接字符串连接
3. 执行导出：

```bash
pg_dump -h ep-xxx-xxx.us-east-2.aws.neon.tech \
  -U neondb_owner \
  -d neondb \
  -f backup.sql
```

#### 使用 Neon 控制台

1. 登录 [Neon Dashboard](https://console.neon.tech)
2. 选择项目
3. 点击 **Backups** 查看备份历史
4. 可以创建时间点恢复

### 3. 监控和日志

#### 查看日志

**Render 日志**：
1. 进入 Web Service 页面
2. 点击 **Logs** 标签
3. 查看实时日志

**Neon 监控**：
1. 登录 Neon 控制台
2. 选择项目
3. 查看 **Monitoring**：
   - 活跃连接数
   - 查询性能
   - 存储使用量

### 4. 数据库管理工具

推荐使用以下工具连接 Neon 数据库：

| 工具 | 平台 | 说明 |
|------|------|------|
| **DBeaver** | Windows/Mac/Linux | 免费，支持 PostgreSQL |
| **pgAdmin** | Windows/Mac/Linux | 官方推荐 |
| **TablePlus** | Mac/Windows | 付费但很好用 |
| **Neon Console** | Web | 内置 SQL 编辑器 |

连接信息：
- **Host**: 从 Neon 控制台复制
- **Port**: 5432
- **Database**: neondb
- **User**: neondb_owner
- **Password**: 你的密码

### 5. 性能优化

#### Neon 设置

在 Neon 控制台可以调整：

| 设置 | 建议值 | 说明 |
|------|--------|------|
| **Autosuspend** | `5 minutes` | 空闲后自动暂停（节省资源）|
| **Compute Size** | `0.25 vCPU` | 免费版固定 |
| **History Retention** | `7 days` | 查询历史保留天数 |

#### 应用优化

1. **使用连接池**：Neon 的 Pooled Connection 已内置
2. **批量操作**：减少数据库往返
3. **添加索引**：对常用查询字段添加索引

---

## 常见问题排查

### Q1: 数据库连接失败

**症状**：
```
Error: Connection refused
Error: password authentication failed
```

**排查步骤**：
1. ✅ 检查 DATABASE_URL 是否正确
2. ✅ 确认密码正确（注意特殊字符）
3. ✅ 确认包含 `?sslmode=require`
4. ✅ 检查 Neon 项目状态是否正常

**解决方法**：
1. 在 Neon 控制台重新复制连接字符串
2. 确认 Render 环境变量已保存
3. 重启 Render 服务

### Q2: CORS 错误

**症状**：
```
Access to fetch at 'https://word-helper-api.onrender.com' 
from origin 'https://word-helper.vercel.app' has been blocked by CORS policy
```

**排查步骤**：
1. ✅ 检查后端 CORS 配置
2. ✅ 确认前端 API 地址正确
3. ✅ 检查 VITE_API_URL 环境变量

**解决方法**：
后端已配置支持 Vercel 域名，如果仍有问题：
1. 检查前端 `api.ts` 中的 baseURL
2. 确认使用完整的 API 地址（带 https://）
3. 重新部署前端

### Q3: 构建失败

**症状**：
```
npm install failed
npm run build failed
```

**排查步骤**：
1. ✅ 查看 Render 构建日志
2. ✅ 检查 package.json 依赖
3. ✅ 确认 tsconfig.json 配置

**解决方法**：
1. 本地运行 `npm install && npm run build` 测试
2. 检查是否有 TypeScript 错误
3. 确认所有依赖都正确

### Q4: 页面空白

**症状**：
- 访问前端显示空白页
- 控制台有错误

**排查步骤**：
1. ✅ 检查浏览器控制台错误
2. ✅ 确认 API 请求是否成功
3. ✅ 检查网络请求

**解决方法**：
1. 检查 `api.ts` 中的 API 地址
2. 确认后端服务正在运行
3. 查看 Render 日志确认后端状态

### Q5: 服务无法访问

**症状**：
```
This site can't be reached
Connection timed out
```

**排查步骤**：
1. ✅ 检查 Render 服务状态
2. ✅ 确认部署是否完成
3. ✅ 检查服务是否休眠

**Render 休眠说明**：
- 免费版 15 分钟无请求会自动休眠
- 休眠后再次访问需要 30 秒启动
- 这是正常的，不是错误

### Q6: 数据库存储超限

**症状**：
```
ERROR: disk full
Storage limit exceeded
```

**排查步骤**：
1. ✅ 登录 Neon 控制台
2. ✅ 查看存储使用量

**解决方法**：
1. 删除不必要的数据
2. 清理旧的导入记录
3. 考虑付费扩容或清理数据

### Q7: 502 Bad Gateway

**症状**：
```
502 Bad Gateway
```

**排查步骤**：
1. ✅ 检查后端服务是否崩溃
2. ✅ 查看 Render 日志
3. ✅ 确认数据库连接正常

**解决方法**：
1. 重启 Render 服务
2. 检查日志定位问题
3. 如果数据库连接问题，参考 Q1

---

## 成本说明

### 当前部署成本

使用免费方案：

| 服务 | 费用 | 说明 |
|------|------|------|
| **Neon 数据库** | ¥0 | **永久免费**，3GB 存储 |
| **Render 后端** | ¥0 | 免费版，750 小时/月 |
| **Vercel 前端** | ¥0 | 免费版，100GB/月 |

**总计**: ¥0/月

### 付费升级建议

如果需要更好的性能：

| 升级项 | 费用 | 收益 |
|--------|------|------|
| **Neon Neon** | ¥0 → ¥7/月 | 10GB 存储，更多功能 |
| **Render Starter** | ¥0 → ¥7/月 | 永不休眠，更强性能 |
| **Vercel Pro** | ¥0 → ¥20/月 | 更多带宽和功能 |

### 何时升级

建议在以下情况升级：
- 🚀 用户量增长
- 💰 需要更好的性能
- 🔒 需要更高的 SLA
- 💾 存储空间不足

---

## 重要提醒

### ⚠️ 安全建议

1. **密码保管**
   - ✅ DATABASE_URL 保存在环境变量中
   - ✅ 不要将密码硬编码到代码
   - ✅ 定期轮换数据库密码

2. **CORS 配置**
   - 后端已配置支持以下域名：
     - `localhost:3000`（本地开发）
     - `localhost:5173`（Vite 开发）
     - `*.onrender.com`（Render）
     - `*.vercel.app`（Vercel）
     - `*.netlify.app`（Netlify）

3. **数据备份**
   - ✅ 定期导出数据库备份
   - ✅ 重要数据不要只存一份
   - ✅ 测试环境验证备份可用性

### ⚠️ 性能注意

1. **冷启动延迟**
   - Render 免费版休眠后需 30 秒启动
   - 这是正常的，无法完全避免
   - 可以使用监控服务保持活跃

2. **数据库限制**
   - Neon 免费版限制 3GB 存储
   - 超出后需要清理或付费
   - 定期检查存储使用量

3. **网络延迟**
   - 亚太用户访问速度较快
   - 美东用户可能稍慢
   - 整体可接受

### ⚠️ 定期维护

建议每月检查一次：

- [ ] 查看 Neon 存储使用量
- [ ] 检查 Render 服务状态
- [ ] 测试备份是否可用
- [ ] 确认所有功能正常
- [ ] 清理不必要的测试数据

---

## 联系和支持

### 文档信息

- **文档版本**: 2.0.0
- **最后更新**: 2026-05-23
- **适用项目**: Word Helper v1.0+

### 外部资源

| 资源 | 链接 |
|------|------|
| **Neon 文档** | https://neon.tech/docs |
| **Render 文档** | https://render.com/docs |
| **Vercel 文档** | https://vercel.com/docs |
| **PostgreSQL 文档** | https://www.postgresql.org/docs |

### 遇到问题

1. 📖 查看本文档的排查部分
2. 🔍 搜索错误信息
3. 📝 查看各平台的官方文档
4. 💬 在 GitHub 提交 Issue

---

## 部署完成检查清单

- [ ] GitHub 账号准备
- [ ] Neon 数据库创建
- [ ] Neon 连接字符串保存
- [ ] Render 后端部署
- [ ] 环境变量配置（DATABASE_URL）
- [ ] 后端健康检查通过
- [ ] Vercel 前端部署
- [ ] 环境变量配置（VITE_API_URL）
- [ ] 前端登录测试
- [ ] 基本功能测试（导入、练习）

---

## 快速参考

### 常用命令

```bash
# 推送代码
git add .
git commit -m "更新"
git push origin main

# 本地测试构建
cd backend
npm install && npm run build

# 本地测试后端
npm start
```

### 重要地址

| 服务 | 地址 |
|------|------|
| **Neon Dashboard** | https://console.neon.tech |
| **Render Dashboard** | https://dashboard.render.com |
| **Vercel Dashboard** | https://vercel.com/dashboard |

### 环境变量速查

**后端 (Render)**:
```
NODE_ENV=production
PORT=10000
DATABASE_URL=postgresql://user:pass@ep-xxx.neon.tech/neondb?sslmode=require
PGSSLMODE=require
```

**前端 (Vercel)**:
```
VITE_API_URL=https://word-helper-api.onrender.com
```

---

**祝部署顺利！** 🎉

---

*本文档由 AI 自动生成，如有问题欢迎反馈。*
